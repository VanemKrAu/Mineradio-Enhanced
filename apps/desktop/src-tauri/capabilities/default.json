{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal Mineradio runtime shell capabilities for the phase-5 Tauri runtime. No filesystem, shell-execute, dialog, or updater permissions are granted in this phase.",
  "windows": ["main"],
  "permissions": [
    "core:window:default",
    "core:event:default",
    "core:webview:default",
    "core:app:default"
  ]
}